#include <stdio.h>

int main() {
    char s[6] = "Hello"; // ici on a besoin d'avoir plus de place pour que "hello" rentre, il y a plus de 3 caracteres dans hello
    printf("%s\n", s);
    return 0;
}
